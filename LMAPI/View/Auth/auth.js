// Arquivo de autenticação (pode ser usado para funcionalidades adicionais de autenticação)
// As funções principais estão em api-auth.js

// Verificar se o usuário está autenticado ao carregar a página
document.addEventListener('DOMContentLoaded', function() {
    // Esta função pode ser usada para verificar autenticação em páginas protegidas
    // Por enquanto, deixamos vazio pois a lógica principal está em api-auth.js
});

